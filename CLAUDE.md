# git
- never commit personal information files.
- before `git push`, verifed files are without personal, sensitive files.

# add, modify anything
- read `DESIGN.md` first to understand the whole picture of this program.

# design changed, feature added
- ask user if needs to create or update design spec (`DESIGN.md`) for LLM-agent
  reference in the future.
- minor changes, bug fixed may not impact design, your call, update or not.

# debug
- read `DEBUG.md` first.
- once bug fixed and confirmed by user, ask user if needs to update `DEBUG.md`,
  which means, don't update DEBUG.md automatically except you 99.9% fixed
  trivial bugs and worth to note.

# ibkr gateway permission
- forbidden to send any `write` permission request.
